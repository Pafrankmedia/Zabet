import ContactForm from "@/components/ContactForm";

export const metadata = {
  title: "Contact ZTKL | Connect with Our Global Advisory Offices",
  description: "Get in touch with ZTKL partners in London, Lagos, or Nairobi. Initiate a consultation or request our brand profile.",
};

export default function Contact() {
  return <ContactForm />;
}
